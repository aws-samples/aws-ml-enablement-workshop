# Values are populated during the automated deployment pipeline.
# For local testing, set these to the desired environment endpoints before running `npm run build`.
VITE_API_ENDPOINT=
VITE_API_KEY=
VITE_ENVIRONMENT=production
VITE_AWS_REGION=us-east-1
