{
  "name": "mlew-tracker",
  "version": "1.0.0",
  "description": "Analytics tracking system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:dashboard": "npm run dev --workspace=packages/dashboard",
    "build": "npm run build --workspaces",
    "build:sdk": "npm run build --workspace=packages/tracker-sdk",
    "build:dashboard": "npm run build --workspace=packages/dashboard",
    "build:cdk": "npm run build --workspace=packages/cdk",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "deploy": "npm run deploy --workspace=packages/cdk",
    "deploy:with-config": "npm run deploy:with-config --workspace=packages/cdk",
    "cdk": "npm run cdk --workspace=packages/cdk"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "nanoid": "^5.1.5"
  }
}
